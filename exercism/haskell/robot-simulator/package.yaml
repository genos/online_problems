name: robot-simulator
version: 2.0.0.4

dependencies:
  - base

library:
  exposed-modules: Robot
  source-dirs: src
  dependencies: []

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - robot-simulator
      - hspec
