name: scrabble-score
version: 1.0.0.3

dependencies:
  - base

library:
  exposed-modules: Scrabble
  source-dirs: src
  dependencies:
    - containers

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - scrabble-score
      - hspec
